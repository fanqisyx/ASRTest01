<!--
使用说明书
版本: v1.7.0
生成时间: 2025-08-29
若修改本文档请同步更新版本号与日期。
-->

# 多组件语音助手系统 使用说明书 (v1.7.0)

更新时间：2025-08-29  
适用系统：Windows 10/11 64-bit  
组件：ASR（语音识别） / LLM（大模型交互） / TTS（语音播报） – 各自独立运行，可单独打包发布。

---
## 1. 系统概述
本系统将语音链路拆分成三个松耦合进程，通过单行 SQLite 数据库（events 表）+ 可选 MQTT 传递消息：
- ASR 捕获一句（唤醒后第一句）写入 ASR DB（仅一行覆盖）。
- LLM 轮询 ASR DB time 变化 -> 读取 parameter.listened -> 动态读取系统提示词 -> 调用 LM Studio -> 结果写入 LLM DB（单行）。
- TTS 轮询 LLM DB time 变化 -> 解析 parameter JSON 的 play 字段 -> 发声。

优势：结构清晰、组件可热重启、资源占用隔离、无需复杂队列即可实现最小事件通路。

---
## 2. 快速上手
1. 安装依赖：分别进入 `ASR_App/`, `LLM_App/`, `TTS_App/` 运行 `pip install -r requirements.txt`。
2. 启动 LM Studio 并加载所需模型（保证 URL 和模型名匹配）。
3. 按顺序启动：TTS -> LLM -> ASR（顺序不是强制，但这样可立即听到响应）。
4. （若开启唤醒）先说唤醒词；听到提示音后说指令一句。未开启唤醒则直接说。
5. 观察：LLM 回复 -> TTS 播放；数据库对应 events 表仅保留最新一条。

---
## 3. 目录与核心文件
| 目录 | 说明 |
|------|------|
| ASR_App | 语音识别程序及其配置、Vosk 模型路径设置 |
| LLM_App | 大模型调用（LM Studio 接口）、系统提示词文件、LLM DB |
| TTS_App | 语音合成程序（pyttsx3）|
| 打包_*.bat | 各组件 PyInstaller 打包脚本 |
| System_Prompt.txt | （主根目录或 LLM_App 下）可被 LLM 动态读取的系统提示词 |

---
## 4. 数据流与事件格式
所有组件间主通路使用单行 events 表：
```
id | time                | command | parameter
---+---------------------+---------+--------------------------------
 1 | 2025-08-29 11:32:10 | listened| {"listened":"用户说的话"}
```
LLM 输出（非 JSON 回复包装）：
```
{"command":"play","play":"模型原文回答"}
```
TTS 仅播报解析出的 JSON.play。

---
## 5. 运行模式
| 模式 | 描述 | 适用场景 |
|------|------|----------|
| 纯 SQLite | 默认；仅依赖本地 DB | 离线/最少依赖 |
| SQLite + MQTT | 双通道 | 调试、外部监听消息 |
| 旧任务队列兼容 | LLM/TTS 继续消费 tasks 表 | 渐进迁移阶段 |

---
## 6. 关键配置参数
### 6.1 通用
| 参数 | 说明 |
|------|------|
| enable_sqlite | 是否启用 SQLite 通道（写/读事件或旧 tasks）|
| enable_mqtt | 是否启用 MQTT 发布/订阅 |
| autostart | GUI 打开后自动开始运行 |

### 6.2 ASR_App/config.json
| 参数 | 说明 |
|------|------|
| vosk_model_path | Vosk 模型目录 |
| enable_wakeword | 是否启用唤醒流程 |
| wakeword | 唤醒词文本（例："小溪"）|
| wake_beep_path | 提示音文件（wav/mp3 可选）|
| mic_guard_file | 控制录音文件；内容为 '1' 才收音 |
| mic_guard_interval | 轮询麦克风开关文件间隔秒 |
| pinyin_fuzzy_hf | 拼音模糊（f/h 合并）|

### 6.3 LLM_App/config.json
| 参数 | 说明 |
|------|------|
| lmstudio_url | LM Studio API 地址 |
| lmstudio_model | 模型名 |
| system_prompt_path | 每次调用前读取的系统提示词 TXT |
| asr_db_path | ASR 事件数据库路径（只读）|
| llm_db_path | LLM 自身事件数据库路径（只写）|
| asr_poll_interval | 轮询 ASR DB 间隔秒（响应速度）|
| no_think | 追加 /no_think 抑制长思考（视模型支持）|

### 6.4 TTS_App/config.json
| 参数 | 说明 |
|------|------|
| llm_db_path | 轮询的 LLM DB 路径（优先）|
| tts_poll_interval | 轮询间隔（缺省使用 asr_poll_interval 或 0.5）|
| enable_sqlite | 若开启，兼容旧 tasks 队列 |

---
## 7. 唤醒机制（ASR）
1. 未唤醒：识别结果仅 GUI 显示，不写 DB。  
2. 匹配唤醒词：播放提示音（可选） -> 状态置“唤醒”  
3. 下一句：写入 DB（覆盖），恢复为未唤醒。  
4. 关闭唤醒：直接每句覆盖写入（始终只有一行）。

---
## 8. LLM 回复解析策略
| 情况 | 行为 |
|------|------|
| 合法 JSON | command=JSON.command, parameter=JSON.parameter（若无则整个 JSON）|
| 非 JSON | 自动包装成 {"command":"play","play":"原文"} |
写入 DB 时仅保留最新一条（DELETE + INSERT）。

---
## 9. TTS 播放逻辑
1. 轮询 LLM DB events 第一行。  
2. 检测 time 变化。  
3. parameter 解析 JSON，取 play 字段非空文本播放。  
4. 无 play 或为空 -> 跳过。  
5. 若启用旧 tasks 或 MQTT，可同时消费。

---
## 10. 性能与并发
已启用：WAL 模式 + busy_timeout + 短事务（BEGIN IMMEDIATE -> COMMIT）减少文件锁。读取端快速开关连接，避免长时间持锁。

---
## 11. 打包方式
根目录提供：
```
打包_ASR_GUI.bat
打包_LLM_GUI.bat
打包_TTS_GUI.bat
打包_全部_GUI.bat
```
执行后生成 `dist/<App>/...exe`。  
附加资源通过 `--add-data` 已包含（需自带 Vosk 模型时可手动添加）。

---
## 12. 故障速查
| 现象 | 排查步骤 |
|------|----------|
| LLM 不触发 | 是否 ASR 未写入 (唤醒未完成 / mic_guard.txt !=1) |
| TTS 不播报 | LLM DB parameter 无 play 或 time 未变 |
| 频繁 BUSY | 确认未长时间打开 DB；WAL 已启用，如仍异常重启应用 |
| 回复不是 JSON | 调整系统提示词，明确输出格式 |

---
## 13. 测试用最小步骤
1. 清空 asr_app.db / llm_app.db（可删除）。  
2. 启动 TTS -> LLM -> ASR。  
3. 说：唤醒词 + “今天天气”。  
4. 验证：ASR DB parameter.listened 存在；LLM DB parameter.play 存在；听到播报。

---
## 14. JSON 示例
ASR:
```
{"listened":"播放音乐"}
```
LLM（非 JSON 原始回答 -> 包装）:
```
{"command":"play","play":"好的，正在为你播放。"}
```
TTS：仅播报 play。

---
## 15. 版本记录
| 版本 | 日期 | 说明 |
|------|------|------|
| v1.7.0 | 2025-08-29 | 拆分三进程、单行事件表、WAL 优化、TTS 仅播 play |
| v1.6.x | 2025-08 | 过渡版，增加部分分离逻辑 |
| v1.4.x 及更早 | 2025-07 | 单体应用形态 |

---
## 16. 免责声明
本软件仅用于内部测试与技术验证，不对第三方数据与模型结果承担保证责任。部署到生产请评估安全、合规与隐私要求。

---
如需：纯 MQTT 模式 / 日志持久化 / 命令扩展（暂停、音量）请反馈需求。

© 2025 语音助手系统 v1.7.0
